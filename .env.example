DISCORD_TOKEN=your_discord_bot_token_here
# Optional: route YouTube traffic through a residential-IP HTTP proxy so
# YouTube's datacenter-IP bot detection doesn't block extraction on the VPS.
# e.g. a tinyproxy on a Tailscale residential device. Leave unset to disable.
YT_PROXY=http://100.114.153.17:8888
