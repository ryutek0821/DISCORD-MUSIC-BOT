# Keep the Discord voice stack on a known-compatible release.  The ``voice``
# extra installs davey (required by discord.py 2.7+) and PyNaCl.
discord.py[voice]==2.7.1
davey>=0.1.4,<0.2
PyNaCl>=1.5.0,<2

# Upper bounds prevent an unrelated upstream release from silently breaking
# production; update these intentionally during dependency maintenance.
yt-dlp>=2025.9.26,<2027
python-dotenv>=1.0.0,<2
requests>=2.31.0,<3
selenium>=4.40.0,<5
